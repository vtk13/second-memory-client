var assert = require('chai').assert;

describe('sample test suite', function() {
    beforeEach(function() {
        // some init code
    });

    it('sample test', function() {
        assert.ok(true);
    });
});
